export class Proyectos{
  id:number;
  name:string;
  info:string;
  image:string;

 constructor(id:number, name:string, info:string, image:string)
 {

     this.id=id;
     this.name=name;
     this.info=info;
     this.image=image;
 }

}
